-- V1: baseline migration.
-- Intentionally empty: no business tables exist yet. Running this migration
-- only establishes the flyway_schema_history version table. Business schema
-- arrives in later, design-approved migrations.
SELECT 1;
