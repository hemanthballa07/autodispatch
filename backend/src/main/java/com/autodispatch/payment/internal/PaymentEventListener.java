package com.autodispatch.payment.internal;

import org.springframework.stereotype.Component;

@Component
class PaymentEventListener {
    // Reserved for future cross-module events (e.g., ride completed → auto-initiate fare).
}
